services:
  web:
    build: .
    container_name: watch-party-board
    environment:
      WATCH_PARTY_DB_PATH: /app/data/app.db
    ports:
      - "7000:8000"
    volumes:
      - watch_party_data:/app/data
    restart: unless-stopped

volumes:
  watch_party_data:
